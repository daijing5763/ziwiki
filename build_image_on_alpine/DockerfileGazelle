FROM  gazelle_image_alpine
WORKDIR /ziwiki
COPY . .
RUN export PATH="$PATH:$HOME/bin" \
  && export GOPROXY=https://goproxy.cn/ \
  && bazel run //:gazelle \
  && bazel run //:gazelle -- update-repos -from_file=go.mod -to_macro=deps.bzl%go_dependencies 
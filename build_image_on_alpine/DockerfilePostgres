# Build stage
FROM postgres:14-alpine
WORKDIR /app
COPY pg_jieba .

COPY deps_postgres.sh .
RUN chmod +x deps_postgres.sh  \
  && sh deps_postgres.sh 


RUN mkdir build && \ 
  cd build && \
  cmake .. && \
  make && \
  make install  
